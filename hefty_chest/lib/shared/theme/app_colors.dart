import 'package:flutter/material.dart';

/// App color palette matching HTML mockups exactly
class AppColors {
  AppColors._();

  // Backgrounds
  static const bgPrimary = Color(0xFF0A0E1A);
  static const bgSecondary = Color(0xFF111827);
  static const bgCard = Color(0xFF151C2C);
  static const bgCardInner = Color(0xFF1A2235);

  // Accents
  static const accentBlue = Color(0xFF4F5FFF);
  static const accentBlueHover = Color(0xFF6470FF);
  static const accentGreen = Color(0xFF22C55E);
  static const accentRed = Color(0xFFEF4444);
  static const accentOrange = Color(0xFFF59E0B);
  static const accentPurple = Color(0xFFA855F7);
  static const accentCyan = Color(0xFF06B6D4);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF9CA3AF);
  static const textMuted = Color(0xFF5A6478);

  // Border
  static const borderColor = Color(0xFF2D3548);

  // Superset indicator
  static const supersetBorder = accentBlue;
}
