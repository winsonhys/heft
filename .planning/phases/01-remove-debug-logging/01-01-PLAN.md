---
phase: 01-remove-debug-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HeftyBack/internal/handlers/session.go
autonomous: true
requirements:
  - CLEN-01

must_haves:
  truths:
    - "Starting a session produces no debug output on the server's stderr stream"
    - "The session handler source contains no fmt.Fprintf or fmt.Fprintln calls"
    - "The session handler source contains no log.Printf debug statements"
    - "All existing session handler tests pass after the change"
  artifacts:
    - path: "HeftyBack/internal/handlers/session.go"
      provides: "Clean session handler without debug logging"
      contains_not: "fmt.Fprintf|fmt.Fprintln|log.Printf.*DEBUG|log.Printf.*SyncSession"
  key_links:
    - from: "HeftyBack/internal/handlers/session.go"
      to: "HeftyBack/internal/handlers/session_test.go"
      via: "unit tests cover handler behavior"
      pattern: "go test.*handlers"
---

<objective>
Remove all debug logging from the session handler.

Purpose: The session handler contains 5 `fmt.Fprintf`/`fmt.Fprintln` calls writing raw debug output to stderr, plus 7 `log.Printf` debug statements in SyncSession. None of these are production-appropriate. Removing them cleans up the handler and eliminates noisy output from the running server.

Output: A clean `session.go` with no debug logging and unused imports (`fmt`, `os`, `log`) removed.
</objective>

<execution_context>
@/Users/winson.heng/.claude/get-shit-done/workflows/execute-plan.md
@/Users/winson.heng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@HeftyBack/internal/handlers/session.go
@HeftyBack/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all debug logging from session handler</name>
  <files>HeftyBack/internal/handlers/session.go</files>
  <action>
Remove ALL debug logging statements from `HeftyBack/internal/handlers/session.go`. This includes two categories:

**Category 1 — fmt.Fprintf/fmt.Fprintln (required by CLEN-01):**
Delete these 5 lines entirely (do not replace with anything):
- Line 36: `fmt.Fprintln(os.Stderr, "[StartSession] HANDLER CALLED VIA FMT...")`
- Line 37: `fmt.Fprintf(os.Stderr, "[StartSession] Request: workoutTemplateId=%s\n", ...)`
- Line 110-117: `fmt.Fprintf(os.Stderr, "[StartSession] Processing item: ...")` (multi-line call)
- Line 139: `fmt.Fprintf(os.Stderr, "[StartSession] Adding rest item with duration %d\n", ...)`
- Line 146-147: `fmt.Fprintf(os.Stderr, "[StartSession] SKIPPED item: ...")` (multi-line call)

**Category 2 — log.Printf debug statements (Claude's discretion — removing for clean production code):**
Delete these 7 lines entirely:
- Line 97-98: `log.Printf("DEBUG ITEM: section=%s ...")` (and its continuation)
- Line 308: `log.Printf("[SyncSession] Received deletedSetIds: %v", ...)`
- Line 309: `log.Printf("[SyncSession] Received deletedExerciseIds: %v", ...)`
- Line 316: `log.Printf("[SyncSession] DeleteSets completed for IDs: %v", ...)`
- Line 323: `log.Printf("[SyncSession] DeleteExercises completed for IDs: %v", ...)`
- Line 354: `log.Printf("[SyncSession] Returning session with %d exercises", ...)`
- Line 356: `log.Printf("[SyncSession]   Exercise %s has %d sets", ...)` (and its containing for loop on line 355-357)

**Import cleanup:**
After removing all the above, the `"fmt"`, `"os"`, and `"log"` imports are no longer used by any remaining code in the file. Remove all three from the import block.

**What NOT to do:**
- Do not add replacement logging (structured or otherwise). These are pure debug statements with no operational value.
- Do not modify any business logic. Only delete logging lines and unused imports.
- Do not touch any other handler files.
  </action>
  <verify>
    <automated>cd /Users/winson.heng/heft/HeftyBack && go build ./internal/handlers/ && echo "BUILD OK" && go vet ./internal/handlers/ && echo "VET OK"</automated>
  </verify>
  <done>
- Zero `fmt.Fprintf` or `fmt.Fprintln` calls remain in session.go
- Zero `log.Printf` debug calls remain in session.go
- The `fmt`, `os`, and `log` imports are removed from session.go
- File compiles without errors and passes go vet
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all session handler tests pass</name>
  <files>HeftyBack/internal/handlers/session_test.go</files>
  <action>
Run the existing unit tests for the session handler to confirm the debug logging removal did not break any behavior.

Run: `cd /Users/winson.heng/heft/HeftyBack && make test-unit`

If any test fails, investigate and fix. The only change was removing logging statements — no business logic was modified — so failures would indicate an accidental deletion of a non-logging line. If that happens, compare against the original code and restore the accidentally deleted line.

Do NOT modify any test files unless a test was explicitly testing debug output (unlikely but check).
  </action>
  <verify>
    <automated>cd /Users/winson.heng/heft/HeftyBack && make test-unit</automated>
  </verify>
  <done>
- All unit tests in `internal/handlers/` pass
- No test regressions from the logging removal
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks:

1. `grep -n "fmt\.\|log\.Printf" HeftyBack/internal/handlers/session.go` returns NO matches
2. `cd HeftyBack && go build ./...` succeeds (full project build)
3. `cd HeftyBack && make test-unit` passes all tests
</verification>

<success_criteria>
- session.go contains zero fmt.Fprintf, fmt.Fprintln, or log.Printf calls
- session.go does not import "fmt", "os", or "log"
- All existing tests pass without modification
- No business logic changes — only logging deletion and import cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/01-remove-debug-logging/01-01-SUMMARY.md`
</output>
